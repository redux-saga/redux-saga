"use strict";(globalThis.webpackChunkwebsite=globalThis.webpackChunkwebsite||[]).push([[821],{6384(e,n,t){t.r(n),t.d(n,{assets:()=>i,contentTitle:()=>r,default:()=>d,frontMatter:()=>c,metadata:()=>a,toc:()=>l});const a=JSON.parse('{"id":"advanced/Channels","title":"Channels","description":"Until now we\'ve used the take and put effects to communicate with the Redux Store. Channels generalize those Effects to communicate with external event sources or between Sagas themselves. They can also be used to queue specific actions from the Store.","source":"@site/../docs/advanced/Channels.md","sourceDirName":"advanced","slug":"/advanced/Channels","permalink":"/docs/advanced/Channels","draft":false,"unlisted":false,"editUrl":"https://github.com/redux-saga/redux-saga/edit/main/docs/../docs/advanced/Channels.md","tags":[],"version":"current","frontMatter":{"title":"Channels","hide_title":true},"sidebar":"docs","previous":{"title":"Using Saga Helpers","permalink":"/docs/basics/UsingSagaHelpers"},"next":{"title":"Composing Sagas","permalink":"/docs/advanced/ComposingSagas"}}');var s=t(4848),o=t(8453);const c={title:"Channels",hide_title:!0},r="Using Channels",i={},l=[{value:"Using the <code>actionChannel</code> Effect",id:"using-the-actionchannel-effect",level:2},{value:"Using the <code>eventChannel</code> factory to connect to external events",id:"using-the-eventchannel-factory-to-connect-to-external-events",level:2},{value:"Using channels to communicate between Sagas",id:"using-channels-to-communicate-between-sagas",level:2},{value:"Using the <code>multicastChannel</code> to communicate with <em>different</em> workers",id:"using-the-multicastchannel-to-communicate-with-different-workers",level:2}];function h(e){const n={a:"a",blockquote:"blockquote",code:"code",em:"em",h1:"h1",h2:"h2",header:"header",li:"li",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,o.R)(),...e.components};return(0,s.jsxs)(s.Fragment,{children:[(0,s.jsx)(n.header,{children:(0,s.jsx)(n.h1,{id:"using-channels",children:"Using Channels"})}),"\n",(0,s.jsxs)(n.p,{children:["Until now we've used the ",(0,s.jsx)(n.code,{children:"take"})," and ",(0,s.jsx)(n.code,{children:"put"})," effects to communicate with the Redux Store. Channels generalize those Effects to communicate with external event sources or between Sagas themselves. They can also be used to queue specific actions from the Store."]}),"\n",(0,s.jsx)(n.p,{children:"In this section, we'll see:"}),"\n",(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsxs)(n.li,{children:["\n",(0,s.jsxs)(n.p,{children:["How to use the ",(0,s.jsx)(n.code,{children:"yield actionChannel"})," Effect to buffer specific actions from the Store."]}),"\n"]}),"\n",(0,s.jsxs)(n.li,{children:["\n",(0,s.jsxs)(n.p,{children:["How to use the ",(0,s.jsx)(n.code,{children:"eventChannel"})," factory function to connect ",(0,s.jsx)(n.code,{children:"take"})," Effects to external event sources."]}),"\n"]}),"\n",(0,s.jsxs)(n.li,{children:["\n",(0,s.jsxs)(n.p,{children:["How to create a channel using the generic ",(0,s.jsx)(n.code,{children:"channel"})," factory function and use it in ",(0,s.jsx)(n.code,{children:"take"}),"/",(0,s.jsx)(n.code,{children:"put"})," Effects to communicate between two Sagas."]}),"\n"]}),"\n"]}),"\n",(0,s.jsxs)(n.h2,{id:"using-the-actionchannel-effect",children:["Using the ",(0,s.jsx)(n.code,{children:"actionChannel"})," Effect"]}),"\n",(0,s.jsx)(n.p,{children:"Let's review the canonical example:"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-javascript",children:"import { take, fork, ... } from 'redux-saga/effects'\n\nfunction* watchRequests() {\n  while (true) {\n    const {payload} = yield take('REQUEST')\n    yield fork(handleRequest, payload)\n  }\n}\n\nfunction* handleRequest(payload) { ... }\n"})}),"\n",(0,s.jsxs)(n.p,{children:["The above example illustrates the typical ",(0,s.jsx)(n.em,{children:"watch-and-fork"})," pattern. The ",(0,s.jsx)(n.code,{children:"watchRequests"})," saga is using ",(0,s.jsx)(n.code,{children:"fork"})," to avoid blocking and thus not missing any action from the store. A ",(0,s.jsx)(n.code,{children:"handleRequest"})," task is created on each ",(0,s.jsx)(n.code,{children:"REQUEST"})," action. So if there are many actions fired at a rapid rate there can be many ",(0,s.jsx)(n.code,{children:"handleRequest"})," tasks executing concurrently."]}),"\n",(0,s.jsxs)(n.p,{children:["Imagine now that our requirement is as follows: we want to process ",(0,s.jsx)(n.code,{children:"REQUEST"})," serially. If we have at any moment four actions, we want to handle the first ",(0,s.jsx)(n.code,{children:"REQUEST"})," action, then only after finishing this action we process the second action and so on..."]}),"\n",(0,s.jsxs)(n.p,{children:["So we want to ",(0,s.jsx)(n.em,{children:"queue"})," all non-processed actions, and once we're done with processing the current request, we get the next message from the queue."]}),"\n",(0,s.jsxs)(n.p,{children:["Redux-Saga provides a little helper Effect ",(0,s.jsx)(n.code,{children:"actionChannel"}),", which can handle this for us. Let's see how we can rewrite the previous example with it:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-javascript",children:"import { take, actionChannel, call, ... } from 'redux-saga/effects'\n\nfunction* watchRequests() {\n  // 1- Create a channel for request actions\n  const requestChan = yield actionChannel('REQUEST')\n  while (true) {\n    // 2- take from the channel\n    const {payload} = yield take(requestChan)\n    // 3- Note that we're using a blocking call\n    yield call(handleRequest, payload)\n  }\n}\n\nfunction* handleRequest(payload) { ... }\n"})}),"\n",(0,s.jsxs)(n.p,{children:["The first thing is to create the action channel. We use ",(0,s.jsx)(n.code,{children:"yield actionChannel(pattern)"})," where pattern is interpreted using the same rules we mentioned previously with ",(0,s.jsx)(n.code,{children:"take(pattern)"}),". The difference between the 2 forms is that ",(0,s.jsx)(n.code,{children:"actionChannel"})," ",(0,s.jsx)(n.strong,{children:"can buffer incoming messages"})," if the Saga is not yet ready to take them (e.g. blocked on an API call)."]}),"\n",(0,s.jsxs)(n.p,{children:["Next is the ",(0,s.jsx)(n.code,{children:"yield take(requestChan)"}),". Besides usage with a ",(0,s.jsx)(n.code,{children:"pattern"})," to take specific actions from the Redux Store, ",(0,s.jsx)(n.code,{children:"take"})," can also be used with channels (above we created a channel object from specific Redux actions). The ",(0,s.jsx)(n.code,{children:"take"})," will block the Saga until a message is available on the channel. The take may also resume immediately if there is a message stored in the underlying buffer."]}),"\n",(0,s.jsxs)(n.p,{children:["The important thing to note is how we're using a blocking ",(0,s.jsx)(n.code,{children:"call"}),". The Saga will remain blocked until ",(0,s.jsx)(n.code,{children:"call(handleRequest)"})," returns. But meanwhile, if other ",(0,s.jsx)(n.code,{children:"REQUEST"})," actions are dispatched while the Saga is still blocked, they will queued internally by ",(0,s.jsx)(n.code,{children:"requestChan"}),". When the Saga resumes from ",(0,s.jsx)(n.code,{children:"call(handleRequest)"})," and executes the next ",(0,s.jsx)(n.code,{children:"yield take(requestChan)"}),", the take will resolve with the queued message."]}),"\n",(0,s.jsxs)(n.p,{children:["By default, ",(0,s.jsx)(n.code,{children:"actionChannel"})," buffers all incoming messages without limit. If you want a more control over the buffering, you can supply a Buffer argument to the effect creator. Redux-Saga provides some common buffers (none, dropping, sliding) but you can also supply your own buffer implementation. ",(0,s.jsx)(n.a,{href:"../api#buffers",children:"See API docs"})," for more details."]}),"\n",(0,s.jsx)(n.p,{children:"For example if you want to handle only the most recent five items you can use:"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-javascript",children:"import { buffers } from 'redux-saga'\nimport { actionChannel } from 'redux-saga/effects'\n\nfunction* watchRequests() {\n  const requestChan = yield actionChannel('REQUEST', buffers.sliding(5))\n  ...\n}\n"})}),"\n",(0,s.jsxs)(n.h2,{id:"using-the-eventchannel-factory-to-connect-to-external-events",children:["Using the ",(0,s.jsx)(n.code,{children:"eventChannel"})," factory to connect to external events"]}),"\n",(0,s.jsxs)(n.p,{children:["Like ",(0,s.jsx)(n.code,{children:"actionChannel"})," (Effect), ",(0,s.jsx)(n.code,{children:"eventChannel"})," (a factory function, not an Effect) creates a Channel for events but from event sources other than the Redux Store."]}),"\n",(0,s.jsx)(n.p,{children:"This basic example creates a Channel from an interval:"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-javascript",children:"import { eventChannel, END } from 'redux-saga'\n\nfunction countdown(secs) {\n  return eventChannel(emitter => {\n      const iv = setInterval(() => {\n        secs -= 1\n        if (secs > 0) {\n          emitter(secs)\n        } else {\n          // this causes the channel to close\n          emitter(END)\n        }\n      }, 1000);\n      // The subscriber must return an unsubscribe function\n      return () => {\n        clearInterval(iv)\n      }\n    }\n  )\n}\n"})}),"\n",(0,s.jsxs)(n.p,{children:["The first argument in ",(0,s.jsx)(n.code,{children:"eventChannel"})," is a ",(0,s.jsx)(n.em,{children:"subscriber"})," function. The role of the subscriber is to initialize the external event source (above using ",(0,s.jsx)(n.code,{children:"setInterval"}),"), then routes all incoming events from the source to the channel by invoking the supplied ",(0,s.jsx)(n.code,{children:"emitter"}),". In the above example we're invoking ",(0,s.jsx)(n.code,{children:"emitter"})," on each second."]}),"\n",(0,s.jsxs)(n.blockquote,{children:["\n",(0,s.jsxs)(n.p,{children:["Note: You need to sanitize your event sources as to not pass null or undefined through the event channel. While it's fine to pass numbers through, we'd recommend structuring your event channel data like your redux actions. ",(0,s.jsx)(n.code,{children:"{ number }"})," over ",(0,s.jsx)(n.code,{children:"number"}),"."]}),"\n"]}),"\n",(0,s.jsxs)(n.p,{children:["Note also the invocation ",(0,s.jsx)(n.code,{children:"emitter(END)"}),". We use this to notify any channel consumer that the channel has been closed, meaning no other messages will come through this channel."]}),"\n",(0,s.jsx)(n.p,{children:"Let's see how we can use this channel from our Saga. (This is taken from the cancellable-counter example in the repo.)"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-javascript",children:"import { take, put, call } from 'redux-saga/effects'\nimport { eventChannel, END } from 'redux-saga'\n\n// creates an event Channel from an interval of seconds\nfunction countdown(seconds) { ... }\n\nexport function* saga() {\n  const chan = yield call(countdown, value)\n  try {\n    while (true) {\n      // take(END) will cause the saga to terminate by jumping to the finally block\n      let seconds = yield take(chan)\n      console.log(`countdown: ${seconds}`)\n    }\n  } finally {\n    console.log('countdown terminated')\n  }\n}\n"})}),"\n",(0,s.jsxs)(n.p,{children:["So the Saga is yielding a ",(0,s.jsx)(n.code,{children:"take(chan)"}),". This causes the Saga to block until a message is put on the channel. In our example above, it corresponds to when we invoke ",(0,s.jsx)(n.code,{children:"emitter(secs)"}),". Note also we're executing the whole ",(0,s.jsx)(n.code,{children:"while (true) {...}"})," loop inside a ",(0,s.jsx)(n.code,{children:"try/finally"})," block. When the interval terminates, the countdown function closes the event channel by invoking ",(0,s.jsx)(n.code,{children:"emitter(END)"}),". Closing a channel has the effect of terminating all Sagas blocked on a ",(0,s.jsx)(n.code,{children:"take"})," from that channel. In our example, terminating the Saga will cause it to jump to its ",(0,s.jsx)(n.code,{children:"finally"})," block (if provided, otherwise the Saga terminates)."]}),"\n",(0,s.jsxs)(n.p,{children:["The subscriber returns an ",(0,s.jsx)(n.code,{children:"unsubscribe"})," function. This is used by the channel to unsubscribe before the event source complete. Inside a Saga consuming messages from an event channel, if we want to ",(0,s.jsx)(n.em,{children:"exit early"})," before the event source complete (e.g. Saga has been cancelled) you can call ",(0,s.jsx)(n.code,{children:"chan.close()"})," to close the channel and unsubscribe from the source."]}),"\n",(0,s.jsx)(n.p,{children:"For example, we can make our Saga support cancellation:"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-javascript",children:"import { take, put, call, cancelled } from 'redux-saga/effects'\nimport { eventChannel, END } from 'redux-saga'\n\n// creates an event Channel from an interval of seconds\nfunction countdown(seconds) { ... }\n\nexport function* saga() {\n  const chan = yield call(countdown, value)\n  try {\n    while (true) {\n      let seconds = yield take(chan)\n      console.log(`countdown: ${seconds}`)\n    }\n  } finally {\n    if (yield cancelled()) {\n      chan.close()\n      console.log('countdown cancelled')\n    }\n  }\n}\n"})}),"\n",(0,s.jsxs)(n.p,{children:["Here is another example of how you can use event channels to pass WebSocket events into your saga (e.g.: using socket.io library).\nSuppose you are waiting for a server message ",(0,s.jsx)(n.code,{children:"ping"})," then reply with a ",(0,s.jsx)(n.code,{children:"pong"})," message after some delay."]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-javascript",children:"import { take, put, call, apply, delay } from 'redux-saga/effects'\nimport { eventChannel } from 'redux-saga'\nimport { createWebSocketConnection } from './socketConnection'\n\n// this function creates an event channel from a given socket\n// Setup subscription to incoming `ping` events\nfunction createSocketChannel(socket) {\n  // `eventChannel` takes a subscriber function\n  // the subscriber function takes an `emit` argument to put messages onto the channel\n  return eventChannel(emit => {\n\n    const pingHandler = (event) => {\n      // puts event payload into the channel\n      // this allows a Saga to take this payload from the returned channel\n      emit(event.payload)\n    }\n\n    const errorHandler = (errorEvent) => {\n      // create an Error object and put it into the channel\n      emit(new Error(errorEvent.reason))\n    }\n\n    // setup the subscription\n    socket.on('ping', pingHandler)\n    socket.on('error', errorHandler)\n\n    // the subscriber must return an unsubscribe function\n    // this will be invoked when the saga calls `channel.close` method\n    const unsubscribe = () => {\n      socket.off('ping', pingHandler)\n    }\n\n    return unsubscribe\n  })\n}\n\n// reply with a `pong` message by invoking `socket.emit('pong')`\nfunction* pong(socket) {\n  yield delay(5000)\n  yield apply(socket, socket.emit, ['pong']) // call `emit` as a method with `socket` as context\n}\n\nexport function* watchOnPings() {\n  const socket = yield call(createWebSocketConnection)\n  const socketChannel = yield call(createSocketChannel, socket)\n\n  while (true) {\n    try {\n      // An error from socketChannel will cause the saga jump to the catch block\n      const payload = yield take(socketChannel)\n      yield put({ type: INCOMING_PONG_PAYLOAD, payload })\n      yield fork(pong, socket)\n    } catch(err) {\n      console.error('socket error:', err)\n      // socketChannel is still open in catch block\n      // if we want end the socketChannel, we need close it explicitly\n      // socketChannel.close()\n    }\n  }\n}\n"})}),"\n",(0,s.jsxs)(n.blockquote,{children:["\n",(0,s.jsxs)(n.p,{children:["Note: messages on an eventChannel are not buffered by default. You have to provide a buffer to the eventChannel factory in order to specify buffering strategy for the channel (e.g. ",(0,s.jsx)(n.code,{children:"eventChannel(subscriber, buffer)"}),").\n",(0,s.jsx)(n.a,{href:"../api#buffers",children:"See the API docs"})," for more info."]}),"\n"]}),"\n",(0,s.jsxs)(n.p,{children:["In this WebSocket example, the socketChannel may emit an error when some socket error occurs, this will abort our ",(0,s.jsx)(n.code,{children:"yield take(socketChannel)"})," waiting on this eventChannel. Note that emitting an error will not abort the channel by default, we need to close the channel explicitly if we want to end the channel after an error."]}),"\n",(0,s.jsx)(n.h2,{id:"using-channels-to-communicate-between-sagas",children:"Using channels to communicate between Sagas"}),"\n",(0,s.jsxs)(n.p,{children:["Besides action channels and event channels, You can also directly create channels which are not connected to any source by default. You can then manually ",(0,s.jsx)(n.code,{children:"put"})," on the channel. This is handy when you want to use a channel to communicate between sagas."]}),"\n",(0,s.jsx)(n.p,{children:"To illustrate, let's review the former example of request handling."}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-javascript",children:"import { take, fork, ... } from 'redux-saga/effects'\n\nfunction* watchRequests() {\n  while (true) {\n    const {payload} = yield take('REQUEST')\n    yield fork(handleRequest, payload)\n  }\n}\n\nfunction* handleRequest(payload) { ... }\n"})}),"\n",(0,s.jsxs)(n.p,{children:["We saw that the watch-and-fork pattern allows handling multiple requests simultaneously, without limit on the number of worker tasks executing concurrently. Then, we used the ",(0,s.jsx)(n.code,{children:"actionChannel"})," effect to limit the concurrency to one task at a time."]}),"\n",(0,s.jsxs)(n.p,{children:["So let's say that our requirement is to have a maximum of three tasks executing at the same time. When we get a request and there are less than three tasks executing, we process the request immediately, otherwise we queue the task and wait for one of the three ",(0,s.jsx)(n.em,{children:"slots"})," to become free."]}),"\n",(0,s.jsx)(n.p,{children:"Below is an example of a solution using channels:"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-javascript",children:"import { channel } from 'redux-saga'\nimport { take, fork, ... } from 'redux-saga/effects'\n\nfunction* watchRequests() {\n  // create a channel to queue incoming requests\n  const chan = yield call(channel)\n\n  // create 3 worker 'threads'\n  for (var i = 0; i < 3; i++) {\n    yield fork(handleRequest, chan)\n  }\n\n  while (true) {\n    const {payload} = yield take('REQUEST')\n    yield put(chan, payload)\n  }\n}\n\nfunction* handleRequest(chan) {\n  while (true) {\n    const payload = yield take(chan)\n    // process the request\n  }\n}\n"})}),"\n",(0,s.jsxs)(n.p,{children:["In the above example, we create a channel using the ",(0,s.jsx)(n.code,{children:"channel"})," factory. We get back a channel which by default buffers all messages we put on it (unless there is a pending taker, in which the taker is resumed immediately with the message)."]}),"\n",(0,s.jsxs)(n.p,{children:["The ",(0,s.jsx)(n.code,{children:"watchRequests"})," saga then forks three worker sagas. Note the created channel is supplied to all forked sagas. ",(0,s.jsx)(n.code,{children:"watchRequests"})," will use this channel to ",(0,s.jsx)(n.em,{children:"dispatch"})," work to the three worker sagas. On each ",(0,s.jsx)(n.code,{children:"REQUEST"})," action the Saga will put the payload on the channel. The payload will then be taken by any ",(0,s.jsx)(n.em,{children:"free"})," worker. Otherwise it will be queued by the channel until a worker Saga is ready to take it."]}),"\n",(0,s.jsx)(n.p,{children:"All the three workers run a typical while loop. On each iteration, a worker will take the next request, or will block until a message is available. Note that this mechanism provides an automatic load-balancing between the 3 workers. Rapid workers are not slowed down by slow workers."}),"\n",(0,s.jsxs)(n.h2,{id:"using-the-multicastchannel-to-communicate-with-different-workers",children:["Using the ",(0,s.jsx)(n.code,{children:"multicastChannel"})," to communicate with ",(0,s.jsx)(n.em,{children:"different"})," workers"]}),"\n",(0,s.jsxs)(n.p,{children:["In the section above we saw how ",(0,s.jsx)(n.code,{children:"channels"})," can be used to load-balance requests between the ",(0,s.jsx)(n.em,{children:"same"})," worker ",(0,s.jsx)(n.em,{children:"forked"})," multiple times. What if we needed to ",(0,s.jsx)(n.code,{children:"put"})," an action to a channel and have ",(0,s.jsx)(n.em,{children:"several different"})," workers consuming it?"]}),"\n",(0,s.jsx)(n.p,{children:"We may need to pass the incoming request to different workers to perform different side effects."}),"\n",(0,s.jsxs)(n.p,{children:["Here an example using ",(0,s.jsx)(n.code,{children:"channels"})," where we can see the problem: when we ",(0,s.jsx)(n.code,{children:"put"})," in the ",(0,s.jsx)(n.code,{children:"channel"})," with ",(0,s.jsx)(n.code,{children:"yield put(chan, payload)"})," we will always run only ",(0,s.jsx)(n.em,{children:"one"})," worker (",(0,s.jsx)(n.code,{children:"logWorker"})," or ",(0,s.jsx)(n.code,{children:"mainWorker"}),"), not ",(0,s.jsx)(n.em,{children:"both"}),"."]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-javascript",children:"import { channel } from 'redux-saga'\nimport { take, fork, call, put } from 'redux-saga/effects'\n\nfunction* watchRequests() {\n  // create a channel to queue incoming requests\n  const chan = yield call(channel)\n\n  // fork both workers\n  yield fork(logWorker, chan)\n  yield fork(mainWorker, chan)\n\n  while (true) {\n    const { payload } = yield take('REQUEST')\n\n    // put here will reach only one worker, not both!\n    yield put(chan, payload)\n  }\n}\n\nfunction* logWorker(channel) {\n  while (true) {\n    const payload = yield take(channel)\n    // Log the request somewhere..\n    console.log('logWorker:', payload)\n  }\n}\n\nfunction* mainWorker(channel) {\n  while (true) {\n    const payload = yield take(channel)\n    // Process the request\n    console.log('mainWorker', payload)\n  }\n}\n"})}),"\n",(0,s.jsxs)(n.p,{children:["To solve the problem we need to use the ",(0,s.jsx)(n.code,{children:"multicastChannel"})," that will ",(0,s.jsx)(n.em,{children:"broadcast"})," the action to all the workers simultaneously."]}),"\n",(0,s.jsxs)(n.blockquote,{children:["\n",(0,s.jsxs)(n.p,{children:["Note that using ",(0,s.jsx)(n.code,{children:"take"})," with the ",(0,s.jsx)(n.code,{children:"multicastChannel"})," requires us to pass the additional argument ",(0,s.jsx)(n.em,{children:"pattern"})," - that you can use to filter the actions to ",(0,s.jsx)(n.code,{children:"take"}),"."]}),"\n"]}),"\n",(0,s.jsx)(n.p,{children:"See the example below:"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-javascript",children:"import { multicastChannel } from 'redux-saga'\nimport { take, fork, call, put } from 'redux-saga/effects'\n\nfunction* watchRequests() {\n  // create a multicastChannel to queue incoming requests\n  const channel = yield call(multicastChannel)\n\n  // fork different workers\n  yield fork(logWorker, channel)\n  yield fork(mainWorker, channel)\n\n  while (true) {\n    const { payload } = yield take('REQUEST')\n    yield put(channel, payload)\n  }\n}\n\nfunction* logWorker(channel) {\n  while (true) {\n    // Pattern '*' for simplicity\n    const payload = yield take(channel, '*')\n    // Log the request somewhere..\n    console.log('logWorker:', payload)\n  }\n}\n\nfunction* mainWorker(channel) {\n  while (true) {\n    // Pattern '*' for simplicity\n    const payload = yield take(channel, '*')\n    // Process the request\n    console.log('mainWorker', payload)\n  }\n}\n"})})]})}function d(e={}){const{wrapper:n}={...(0,o.R)(),...e.components};return n?(0,s.jsx)(n,{...e,children:(0,s.jsx)(h,{...e})}):h(e)}},8453(e,n,t){t.d(n,{R:()=>c,x:()=>r});var a=t(6540);const s={},o=a.createContext(s);function c(e){const n=a.useContext(o);return a.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function r(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(s):e.components||s:c(e.components),a.createElement(o.Provider,{value:n},e.children)}}}]);